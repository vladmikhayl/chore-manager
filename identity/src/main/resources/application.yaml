spring:
  application:
    name: identity

  datasource:
    url: jdbc:postgresql://localhost:15432/identity
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
    properties:
      hibernate:
        format_sql: true
